#include<stdio.h>
#include<math.h>
int main(void){
    double rad, pai = 3.14, x, y, z, a, b, c;

    printf("角度を入力して下さい：");
    scanf("%lf", &a);

    rad = a/180*pai;
    printf("%f度は%fラジアンです。\n",a,rad);
    x = cos(rad);
    printf("cos(%f)の値は%fです。\n",rad,x);
    x = tan(rad);
    printf("tan(%f)の値は%fです。\n\n",rad,x);

    
    printf("数値を入力して下さい：");
    scanf("%lf", &b);
    y = sqrt(b);
    printf("%fの平方根は%fです。\n\n",b,y);


    printf("数値を入力して下さい：");
    scanf("%lf", &c);
    z = log(c);
    printf("log(%.1f)の値は%fです。\n",c,z);
    z = log10(c);
    printf("log(%.1f)の値は%fです。\n",c,z);

    return 0;
}
