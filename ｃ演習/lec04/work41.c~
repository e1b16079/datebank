#include<stdio.h>
int main(void){
    
    int a;

    printf("aの値を入力して下さい：");
    scanf("%d",&a);

    if(a%2==0&&a%3==0){
      printf("%dは２と３の両方で割り切れる値です。\n",a);
    }
    else{
      printf("%dは条件を満たしていません。\n",a);
    }
return 0;
}
