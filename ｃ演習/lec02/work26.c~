#include<stdio.h>
int main(void){
    int d;
    int t;
    double v;
    
    printf("走った距離（単位：m）:");
    scanf("%d",&d);

    printf("所要時間（単位：s）:");
    scanf("%d",&t);
   
    v=(double)d/t;
    printf("そのときの速度は%.1fm/sです。\n",v);
    return 0;
}
