#include<stdio.h>

int main(void){
    int a;
    int b;
    int c;

    printf("鉛筆の本数を入力してください\n");
    scanf("%d",&a);

    b= a/12;
    c= a%12;

    printf("%d本では、%dダースにまとめられ%d本余ります。\n", a, b, c);

    return 0;
    }
