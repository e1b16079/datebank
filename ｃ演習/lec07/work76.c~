#include<stdio.h>
int main(void){

   int a, b, c, i, j, k;

   printf("指数の指定：");
   scanf("%d", &a);

   for(i=1;i<6;i++){
    b=1;
    c=1; 
    for(j=1;j<=a;j++){
      b=b*i;
    }
    for(k=1;k<=i;k++){
      c=c*k;
    }
   printf("%dの%d乗=%d   %d!=%d\n", i, a, b, i, c);
   }

   return 0;
}
