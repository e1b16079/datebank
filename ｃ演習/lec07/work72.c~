#include<stdio.h>
int main(void){

   int a, i, j;

   printf("表示する段数を入力して下さい：");
   scanf("%d", &a);

   for(i=1;i<=a;i++){
     for(j=1;j<=a;j++){
       if(i+j<=a){
        printf("　");
       }
       else{
       printf("□");
       }
      }
     printf("\n");
   }

   return 0;
}
