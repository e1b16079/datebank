#include<stdio.h>
int main(void){

    int inp, sum = 0;

    printf("値を入力して下さい:");
    scanf("%d", &inp);

    while(inp > 0){
    sum = sum + inp;
    printf("値を入力して下さい:");
    scanf("%d", &inp);}

    printf("入力された値の総和は%dです。\n", sum);

    return 0;
}
